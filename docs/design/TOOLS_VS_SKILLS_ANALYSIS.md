# LurkBot 中 Tools vs Skills 深度分析

**版本**: v1.0
**日期**: 2026-01-31
**文档语言**: 中文

---

## 核心概念速查

**Tools (工具)** = 系统级原子功能 (read, exec, web_search)
**Skills (技能)** = 业务级功能包装 (github, notion, wework)

**简单记忆**:
- Tool = 锤子、螺丝刀（单个工具）
- Skill = 修理电脑（使用多个工具完成任务）

---

## 一、核心概念对比

### 1.1 Tools (工具)

```
定义：     系统级的原子功能单元
位置：     src/lurkbot/tools/builtin/
实现方式： Python 函数 + Pydantic 模型
调用方式： Agent 运行时直接调用
粒度：     细粒度（单一职责）
例子：     read(path), web_search(query)
数量：     22 个内置 Tools
```

**特征**：
- ✅ 原子性 - 每个 Tool 只做一件事
- ✅ 系统级 - 属于 LurkBot 核心功能
- ✅ 直接调用 - Agent 可以直接使用
- ✅ 高性能 - 调用路径短，开销小
- ✅ 强类型 - Pydantic 自动验证参数

### 1.2 Skills (技能)

```
定义：     业务级的功能包装
位置：     skills/ (项目根目录)
实现方式： SKILL.md (Frontmatter) + 可选处理代码
调用方式： 通过 System Prompt 描述给 Agent
粒度：     粗粒度（完整业务场景）
例子：     github (包含多个 git 操作)
数量：     3 个示例 + 可从 ClawHub 安装
```

**特征**：
- ✅ 业务性 - 面向完整的使用场景
- ✅ 可扩展 - 用户可自定义或从 ClawHub 安装
- ✅ 文档化 - SKILL.md 自带完整说明
- ✅ 组合性 - 可组合多个 Tools
- ✅ 配置化 - 支持外部依赖和配置

---

## 二、使用场景决策

### 2.1 使用 Tools 的场景

✅ **系统核心功能** - 所有 Agent 都需要的基础能力
```
read, write, exec, web_search
```

✅ **原子操作** - 单一职责，不可再分
```
memory_search(query)     # 单一功能：搜索记忆
session_spawn(agent_id)  # 单一功能：创建会话
```

✅ **高频调用** - 性能敏感的操作
```
Tool 调用路径更短：Agent → Tool
Skill 调用路径更长：Agent → Skill → Tool
```

### 2.2 使用 Skills 的场景

✅ **完整业务流程** - 需要多个步骤协作
```
创建 GitHub PR：
1. 读取变更文件 (read)
2. 查找相关 Issue (web_search)
3. 生成 PR 描述 (write)
4. 调用 GitHub API (exec)
```

✅ **可扩展功能** - 用户可自定义
```
企业自定义工作流
个人自动化脚本
```

✅ **第三方集成** - 从 ClawHub 安装
```bash
lurkbot skills install notion
lurkbot skills install slack
```

✅ **文档化需求** - 需要详细使用说明
```
安装步骤、配置选项、使用示例
```

---

## 三、实际开发决策树

```
需要实现新功能？
    │
    ├─→ 是核心系统能力？ ──→ 是 ──→ 实现为 Tool
    │                              (src/lurkbot/tools/builtin/)
    │
    ├─→ 是业务场景组合？ ──→ 是 ──→ 实现为 Skill
    │                              (skills/xxx/SKILL.md)
    │
    └─→ 不确定？问自己：
        ├─ 功能会被多个场景复用？ ──→ Tool
        ├─ 需要复杂的参数配置？   ──→ Skill
        ├─ 需要外部依赖？         ──→ Skill
        ├─ 需要详细文档说明？     ──→ Skill
        ├─ 只是简单函数调用？     ──→ Tool
        └─ 用户需要自定义？       ──→ Skill
```

---

## 四、具体例子对比

### 4.1 文件操作 (Tool)

```python
# src/lurkbot/tools/builtin/fs_tools.py
def read(path: str, start_line: int = 0, limit: int = 1000) -> str:
    """读取文件"""
    content = Path(path).read_text()
    lines = content.splitlines()
    return "\n".join(lines[start_line:start_line + limit])
```

**为什么是 Tool？**
- ✅ 原子操作（单一职责）
- ✅ 系统核心功能
- ✅ 高频调用
- ✅ 不需要额外文档

### 4.2 GitHub 集成 (Skill)

```markdown
# skills/github/SKILL.md
---
name: github
description: GitHub repository operations
version: 1.0.0
tools:
  - read
  - write
  - web_search
  - exec
triggers:
  - "create pull request"
  - "review code"
dependencies:
  - gh (GitHub CLI)
install: |
  brew install gh
  gh auth login
---

# GitHub Skill

完整的 GitHub 仓库操作，包括 PR 创建、代码审查、CI 检查等。

## 使用示例
[用户] 创建 PR for branch feature-login
[Bot]  ✅ PR 已创建: https://github.com/owner/repo/pull/42
```

**为什么是 Skill？**
- ✅ 完整业务流程（创建 PR 需要多步骤）
- ✅ 需要外部依赖（gh CLI）
- ✅ 需要详细文档
- ✅ 用户可扩展（可添加自定义工作流）

### 4.3 企业微信集成 (Skill)

```markdown
# skills/wework/SKILL.md
---
name: wework
description: 企业微信消息发送和接收
version: 1.0.0
tools:
  - message  # 复用现有 Tool
config:
  corp_id: ${WEWORK_CORP_ID}
  agent_id: ${WEWORK_AGENT_ID}
  secret: ${WEWORK_SECRET}
triggers:
  - "发送企业微信"
  - "查看企业微信消息"
dependencies:
  - wechatpy
---

# 企业微信 Skill

## 配置
在 ~/.lurkbot/config.yml 中添加企业微信凭证...

## 使用
[用户] 发送企业微信消息给 @张三：项目已完成
[Bot]  ✅ 已发送企业微信消息
```

**为什么是 Skill？**
- ✅ 需要配置（corp_id, secret）
- ✅ 业务场景（企业通信）
- ✅ 可选功能（不是所有用户都需要）
- ✅ 需要安装文档

---

## 五、LurkBot 当前状态

### 5.1 已有 22 个 Tools

| 类别 | Tools | 数量 |
|------|-------|------|
| 执行 | exec, process | 2 |
| 文件系统 | read, write, edit, apply_patch | 4 |
| 会话 | sessions_spawn, sessions_send, sessions_list, sessions_history, session_status, agents_list | 6 |
| 记忆 | memory_search, memory_get | 2 |
| 网络 | web_search, web_fetch | 2 |
| 消息 | message | 1 |
| 自动化 | cron | 1 |
| 系统 | gateway | 1 |
| 媒体 | image, tts | 2 |
| 节点 | nodes | 1 |

**位置**: `src/lurkbot/tools/builtin/`

### 5.2 已有 3 个 Skills

```
skills/
├── github/SKILL.md       # GitHub 操作
├── weather/SKILL.md      # 天气查询
└── web-search/SKILL.md   # Web 搜索
```

### 5.3 待完善

```
🔄 短期 (1-2 周)
├── 为 22 个 Tools 创建 SKILL.md（便于 ClawHub 分发）
├── ClawHub API 客户端
└── CLI: lurkbot skills install/search

🔄 中期 (1-2 月)
├── 引入高优先级 Skills (code-review, file-manager)
├── 国内应用 Skills (企业微信、钉钉、飞书)
└── 应用集成 Skills (notion, slack, trello)
```

---

## 六、总结对照表

| 维度 | Tools | Skills |
|------|-------|--------|
| **定位** | 系统功能层 | 业务应用层 |
| **粒度** | 细（原子操作） | 粗（完整流程） |
| **实现** | Python 函数 | SKILL.md + 代码 |
| **位置** | src/lurkbot/tools/builtin/ | skills/ |
| **调用** | Agent 直接调用 | System Prompt |
| **扩展** | 需要写代码 | 创建 Markdown |
| **文档** | Docstring | SKILL.md 自带 |
| **配置** | 函数参数 | Frontmatter |
| **依赖** | 无 | 可声明 |
| **分发** | 内置 | ClawHub |
| **数量** | 22 个 | 3 个 + ClawHub |

---

**文档结束**
